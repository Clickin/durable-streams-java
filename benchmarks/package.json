{
  "name": "durable-streams-java-benchmarks",
  "version": "1.0.0",
  "description": "Performance benchmarks for durable-streams-java using upstream benchmark suite",
  "type": "module",
  "scripts": {
    "bench:inmemory": "vitest bench inmemory.bench.ts",
    "bench:filestore": "vitest bench filestore.bench.ts",
    "bench:all": "npm run bench:inmemory && npm run bench:filestore"
  },
  "dependencies": {
    "@durable-streams/benchmarks": "^0.1.2",
    "@durable-streams/client": "^0.1.2",
    "cross-env": "^7.0.3",
    "vitest": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "typescript": "^5.0.0"
  }
}
